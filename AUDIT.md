# MetaShield 0.3.8 배포 전 전수 감사

감사일: 2026-08-22
대상: MetaShield 0.3.8 (build 21), macOS 13 이상, arm64/x86_64

## 결론

핵심 제거 엔진, 실제 Finder 전달, 사진 관리 경로, 실패 안전성, 자원 제한, 빌드·패키징은
배포 후보 수준으로 통과했습니다. 최종 무료 직접 배포 DMG의 무결성·중첩 서명·Universal 2·
예상 Gatekeeper 차단도 검증했습니다. 0.3.8은 외부 Automator workflow를 없애 Finder 기능과
사진 공유 확장을 앱 번들 안에만 둡니다.

다만 이 결과가 모든 스테가노그래피 제거를 의미하지는 않습니다. 보이는 RGB 픽셀 자체에 숨긴
임의 데이터는 이미지를 유지하면서 확실히 제거할 수 없습니다. 또한 Developer ID가 없는
ad-hoc 서명본은 업데이트 때 사진 권한을 다시 요청할 수 있고, 깨끗한 별도 Mac 및 VoiceOver
수동 시험은 아직 남아 있습니다. 첫 공개본은 베타로 표시하는 것이 적절합니다.

## 감사 범위와 결과

| 영역 | 결과 | 근거 |
|---|---|---|
| 이미지 정리 | 통과 | 새 단일 프레임 8-bit RGB PNG로 재인코딩 후 재디코딩 |
| PNG 구조 | 통과 | `IHDR` 1개·`IDAT` 1개·`IEND` 1개만 허용, 순서·CRC·zlib 종료·정확한 필터 바이트 검증 |
| 알려진 NovelAI 경로 | 통과 | PNG 텍스트·EXIF·알파/알파 LSB·stealth/signature/FEC 운반면 제거 |
| 알파 LSB 잔존 채널 | 통과 (0.3.2 수정) | straight 알파로 디코딩해 같은 양자화 버킷 안의 알파 차이가 출력 RGB에 남지 않음 |
| 파일 메타데이터 | 통과 | 입력 xattr 미복사; 실제 Apple 스크린샷 속성 제거 회귀 시험 통과 |
| 원본 안전 | 통과 | 실패 입력·기존 출력·사진 임시 원본 보존, 검증 후 같은 파일시스템에서 원자적 `rename` |
| 파일 공격면 | 통과 | `O_NOFOLLOW` 단일-open 스냅샷, inode 변경·심볼릭 링크·하드 링크·비정규 파일·다중 프레임 거부 |
| 권한 보존 | 통과 | 원위치 PNG의 소유자·그룹·POSIX 모드·ACL 보존 시험 통과 |
| 자원 고갈 방어 | 통과 | 호스트 256 MiB/4천만 픽셀/100개, 공유 확장 128 MiB/800만 픽셀/20개 |
| 외부 서비스 정지 | 통과 | 파일 제공자·file promise·PhotoKit 권한/가져오기 60초 제한, 늦은 콜백 중복 완료 방지 |
| Finder | 통과 | 번들 내 AppKit Service 실제 호출, 단일·다중 및 한글·공백·따옴표 이름, PNG 교체·JPEG 사본·정규 PNG 검증 |
| 헤드리스 전달 | 통과 | Finder 전달 최대 창 0개, 처리 후 종료; 직접 실행은 창 1개 |
| 사진 원본 보호 | 통과 | `/var/folders`·`.photoslibrary` 입력을 원위치 교체하지 않고 새 PNG만 생성 |
| 사진 앱/PhotoKit | 조건부 통과 | 실제 임시 경로를 PhotoKit으로 처리해 원본 불변·창 0개·프로세스 종료·크래시 0 확인; add-only 권한 때문에 이번 실행의 보관함 자산을 다시 읽어 바이트 비교하지는 못함 |
| 공유 확장 | 통과 | sandbox·등록·입력 복사 방어·취소·개수/바이트/픽셀/시간 제한·콜백 단일 완료 검사 |
| 설치·제거 | 통과 | DMG 드래그 설치, 최초 실행 뒤 서비스/공유 확장 등록, 앱 휴지통 이동 뒤 서비스 자동 소멸, 재설치 뒤 재등록 확인 |
| 외부 잔여물 | 통과 | workflow·LaunchAgent·로그인 항목을 설치하지 않음; 0.3.7 이하의 정확한 MetaShield workflow만 최초 실행 때 정리 |
| 접근성 코드 | 조건부 통과 | 표준 AppKit 컨트롤, 접근성 이름·도움말·상태 알림, Command-O/Escape 대체 경로 |
| 개인정보 | 통과 | 분석·광고·계정·제3자 SDK 없음, 자동 내려받기·설치 없음 |
| 새 버전 확인 | 통과 (0.3.6 갱신) | 기본 꺼짐, 하루 1회, 리디렉션 거부, 512 KiB 스트리밍 상한, 헤드리스 경로는 알림 권한이 이미 허용된 경우에만 조회하고 8초 안에 종료 |
| 빌드 품질 | 통과 | Swift 6 strict, Debug/Release warnings-as-errors, strict format, plist·zsh 문법 검사 |
| 메모리 안전 | 통과 | AddressSanitizer·ThreadSanitizer·UndefinedBehaviorSanitizer 각각 20/20 |
| 아키텍처 | 통과 | 앱·CLI·공유 확장 모두 arm64/x86_64 |
| DMG | 통과 | HFS+ 이미지 무결성, ad-hoc 중첩 서명, 버전 일치, 체크섬, 예상 Gatekeeper 거부 |

자동 자체 테스트는 (0.3.8 감사 시점 기준) 20/20을 통과했고 손상 PNG 변이 코퍼스 1,024개에서 원본 불변을 확인했습니다.

## 0.3.8에서 수정한 설치 구조와 크래시

- Finder용 Automator workflow와 `Finder 빠른 동작 설치/복구` 버튼을 제거했습니다. Finder 명령은
  앱의 `NSServices`로 제공되며 앱을 한 번 실행하면 등록됩니다. 별도 설치 파일, LaunchAgent,
  로그인 항목, Finder 강제 재시작이 없습니다.
- 메인 앱이 첫 실행만으로 호스트 사진 권한을 요청하던 동작을 제거했습니다. 대신
  `사진 앱 권한 연결…` 버튼이 시스템 공유 선택기를 열고 공유 확장 자체에 설정용 1×1 정규 RGB
  PNG와 전용 타입 마커를 전달합니다. 공유 확장은 타입뿐 아니라 마커 바이트까지 일치할 때만
  사진을 만들지 않고 add-only 권한을 요청하며, 설정 데이터·권한 콜백에는 60초 제한과 취소
  경로가 있습니다. 실제 설치본에서 MetaShield 선택·권한 연결 완료와 메인 앱 창 1개 유지,
  설정 경로의 PhotoKit `performChanges` 미호출을 확인했습니다.
- 0.3.7 이하가 만든 정확한 MetaShield workflow와 해당 `pbs` 항목만 최초 실행 때 정리합니다.
  다른 앱의 서비스 설정은 건드리지 않습니다.
- 이미지 형식 편집기로 등록하던 `CFBundleDocumentTypes`를 제거해 Finder의
  `다음으로 열기 > MetaShield` 중복 항목을 없앴습니다. 사용자가 명시적으로 앱 아이콘에
  이미지를 드래그하거나 `open -a MetaShield <파일>`로 전달하는 창 없는 처리 경로는 유지합니다.
- 사진 앱 임시 경로를 처리할 때 `PHPhotoLibrary.performChanges` 완료 콜백이 Photos의 private
  queue에서 MainActor 격리 클로저를 직접 실행해 `EXC_BREAKPOINT`로 종료되는 현상을 실제로
  재현했습니다. 호스트와 공유 확장 모두 system callback을 `nonisolated`에서 만들고 명시적으로
  MainActor로 이동하도록 수정했습니다.
- 같은 잠복 문제가 있던 알림 권한·상태 콜백도 동일한 경계로 수정했습니다. 업데이트 확인이 켜진
  헤드리스 사진 경로에서 원본 불변, 6.2초 종료, 새 크래시 0을 확인했습니다.
- Finder AppKit Service에 NovelAI 텍스트 청크가 든 PNG를 전달해 원위치 SHA 변경,
  `IHDR·IDAT·IEND` 정규 구조, 창 0개, 잔류 프로세스 0, 새 크래시 0을 확인했습니다.

앱을 휴지통으로 옮기면 실행 코드·Finder Service·공유 확장은 함께 없어집니다. 반면
Gatekeeper 승인, TCC 권한, 최근 실행 기록과 UserDefaults는 macOS가 소유하는 상태라 즉시
삭제된다고 보장할 수 없습니다. 이는 실행 가능한 잔여 프로그램이 아니며 별도 제거 도구를
상주시켜 해결할 성격도 아닙니다. macOS가 보호하는 공유 확장 컨테이너
`~/Library/Containers/kr.metashield.app.share`도 남을 수 있지만 처리 이미지를 영구 보관하지
않으며, 보호를 우회해 삭제하도록 안내하지 않습니다.

## 0.3.7에서 추가한 서명 검증 다운로드

Sparkle과 자동 설치는 도입하지 않았습니다. 앱은 여전히 자기 자신을 교체하지 않습니다.
대신 알림을 눌러 브라우저로 나가던 경로가 검증을 우회한다는 문제를 없앴습니다.

- 릴리스마다 오프라인 Ed25519 키로 서명한 `metashield-update.json` 을 함께 올립니다.
  개인키는 GitHub과 이 Mac 어디에도 상주하지 않습니다.
- 앱은 서명을 **파싱 전에** 검증합니다. 통과하지 못한 바이트는 JSON 리더에 도달하지 않습니다.
- manifest에는 주소가 없습니다. 접속 주소는 전부 앱에 컴파일된 값에서 조합하며, 확인된
  버전 문자열만 경로에 들어갑니다. 파일명도 버전에서 유도한 값과 정확히 일치해야 합니다.
- 메타데이터 요청은 리다이렉트를 계속 거부합니다. 자산 다운로드는 GitHub이 별도 호스트로
  보내기 때문에 HTTPS 리다이렉트를 최대 3회 허용하되, 내용은 서명과 SHA-256으로 검증한
  뒤에만 신뢰합니다.
- 받은 파일은 크기와 SHA-256이 서명된 값과 일치할 때만 다운로드 폴더에 공개합니다.
  비교는 상수 시간으로 수행합니다.
- 저장한 파일에는 브라우저 다운로드와 같은 격리 속성을 붙입니다. 앱이 파일을 건네준다는
  이유로 Gatekeeper 최초 실행 확인이 생략되지 않게 하기 위해서입니다.
- 설치는 사용자가 직접 합니다. 자동 실행·마운트·교체는 없습니다.

한계: 계정이 탈취된 공격자는 **예전 서명 manifest를 그대로 재생**해 새 보안 수정이 있다는
사실을 숨길 수 있습니다. 버전 역행은 앱이 거부하지만 이 억제 자체는 막을 수 없습니다.
또한 사용자가 브라우저로 직접 받아 설치하면 서명 검증 경로 밖입니다.

## 0.3.6에서 추가한 알림 배너

앱을 직접 열어야만 새 버전을 알 수 있던 제약을 없앴습니다. 상주 프로세스(LaunchAgent)는
도입하지 않았습니다. 사용자 홈에 쓰기 가능한 자동 실행 항목을 만드는 것은 이 앱이 지키려는
공격면과 맞바꿀 수 없다고 판단했습니다.

대신 사용자가 MetaShield 를 **실제로 쓸 때**(Finder 서비스·사진 앱 포함) 처리를
마친 뒤 확인하고, 새 버전이 있으면 macOS 알림 배너를 띄웁니다. 조건은 다음과 같습니다.

- 확인 기능이 켜져 있고(기본 꺼짐), 하루 1회 한도를 넘지 않았을 것
- **알림 권한이 이미 허용되어 있을 것.** 헤드리스 실행은 권한 창을 절대 띄우지 않습니다.
  권한 요청은 사용자가 앱 창에서 옵션을 켤 때만 합니다.
- 네트워크가 응답하지 않아도 8초 안에 완료 처리하고 프로세스를 종료합니다.

배너에는 주소가 들어 있지 않습니다. 눌렀을 때 여는 릴리스 페이지는 앱에 고정된 값입니다.
알림 식별자는 버전마다 하나여서 반복 실행해도 배너가 쌓이지 않습니다.

사용자가 요청한 확인(옵션을 켜는 순간)은 하루 1회 제한을 적용하지 않습니다. 자동 확인만
제한합니다.

## 0.3.3에서 추가한 새 버전 확인

자동 내려받기·설치는 넣지 않았습니다. ad-hoc 서명본은 내려받은 번들을 개발자 신원과
묶어 검증할 수 없으므로, 네트워크에서 받은 앱으로 설치본을 교체하는 경로는 검증 불가능한
코드 실행 경로가 됩니다. 대신 다음 조건의 확인 기능만 넣었습니다.

- 기본 꺼짐. 사용자가 앱 창에서 켠 경우에만 동작합니다.
- 대화형 실행에서만, 하루 최대 1회. 헤드리스(Finder 서비스·사진) 경로는 네트워크를
  전혀 쓰지 않는 것을 실측 확인했습니다.
- 조회 URL과 릴리스 페이지 URL은 앱에 고정되어 있고, 응답에서 읽는 값은 `tag_name` 하나뿐이며
  `숫자.숫자.숫자` 형식이 아니면 버립니다. 응답이 주소를 바꿀 수 없습니다.
- 응답은 스트리밍 중 512 KiB를 넘는 즉시 취소하고 리디렉션을 거부합니다. 10초 요청 타임아웃,
  20초 자원 타임아웃, 쿠키·캐시 없는 ephemeral 세션을 사용하며 실패 시 무시합니다.
- 새 버전이 있으면 문구와 버튼만 표시하고, 버튼은 기본 브라우저로 릴리스 페이지를 엽니다.

남는 위험: GitHub 계정이 탈취되면 사용자가 악성 릴리스로 안내될 수 있습니다. 자동 설치가
아니므로 사용자가 직접 내려받아 실행하는 단계가 남지만, 이 위험은 계정 2FA와 향후 Developer ID
전환으로만 실질적으로 줄일 수 있습니다.

## 0.3.5에서 보강한 새 버전 확인 경계

- 옵션을 껐다 켜도 모든 확인 경로가 같은 24시간 제한을 통과하므로 반복 조회가 불가능합니다.
- `URLSessionDataDelegate`가 응답을 점진적으로 받아 512 KiB를 넘는 순간 작업을 취소합니다.
- HTTP 리디렉션을 따르지 않고 최종 응답 URL도 컴파일된 GitHub API 주소와 정확히 비교합니다.
- 사용자가 확인 옵션을 끈 뒤 도착한 늦은 콜백은 업데이트 UI를 다시 표시하지 않습니다.

## 0.3.1에서 발견해 0.3.2에서 고친 결함

0.3.1은 premultiplied 비트맵으로 디코딩한 뒤 **원본 알파로 나눠** straight 색을 복원했습니다.
그 결과 알파의 하위 비트가 출력 RGB에 ±1 잡음으로 남아, 알파 LSB에 심은 payload를 정리된
이미지에서 그대로 복원할 수 있었습니다(64×64 이미지에서 24바이트 메시지 100% 복원, 알파
248–255 구간에서도 256색 중 23색이 원본 알파 값을 노출).

0.3.2는 vImage로 **비-premultiplied(straight) sRGB 샘플**을 직접 받아 양자화된 알파만으로
합성합니다. 알파 256단계가 정확히 17개의 출력값으로만 축약되고(경계 8·24·…·248), 같은
버킷 안의 알파 차이는 출력 픽셀에 전혀 남지 않는 것을 확인했습니다. EXIF 방향 처리는 합성이
끝난 불투명 이미지에서 픽셀 단위로 수행하며 방향 1–8 모두 0.3.1과 바이트 단위로 동일한
결과를 냅니다. 회귀 시험 `알파 하위 비트 은닉 payload 제거`를 추가했고, 이 시험이 0.3.1
코드에서는 실패하는 것도 확인했습니다.

같은 배포본에서 함께 정리한 항목: 새 출력 파일 생성 모드를 `0o666`에서 `0o644`로 낮췄고,
빌드 머신 Xcode 툴체인을 가리키던 `LC_RPATH` 항목을 배포 바이너리에서 제거했습니다.

남는 한계: 양자화 버킷을 넘는 알파 차이(예: 247 대 248)는 실제로 눈에 보이는 투명도 차이라
출력에 반영됩니다. 이는 보이는 픽셀에 심은 정보와 같은 범주이며 아래 '보장 경계'에 해당합니다.

## 실제 성능 표본

- 6,324×6,324 약 40 MP PNG: 약 1.08초, 최대 RSS 약 656 MB, peak footprint 약 484 MB.
- 2,828×2,828 약 8 MP PNG: 약 0.77초, 최대 RSS 약 142 MB, peak footprint 약 99 MB.
- 공유 확장은 macOS의 더 작은 메모리 예산을 고려해 8 MP로 별도 제한했습니다.

측정값은 이 감사 Mac의 표본이며 모든 기기의 상한을 보장하지 않습니다.

## 보안 설계

- 모든 이미지 처리는 로컬 Apple 프레임워크와 zlib로 수행합니다. 유일한 네트워크 클라이언트는
  기본으로 꺼진 GitHub 버전 확인이며 이미지나 이미지 메타데이터를 전송하지 않습니다.
- 소스는 `O_NOFOLLOW`로 한 번 열고 `fstat`한 동일 inode를 mmap해 검사·디코딩합니다.
- 원위치 교체 직전 경로의 device/inode/size/mtime을 다시 확인해 경로 교체 경쟁을 거부합니다.
- 임시 결과는 예측 불가능한 이름과 `O_EXCL`로 만들고, 디스크 동기화·정규 구조 검사·재디코딩
  후에만 원자적으로 교체합니다.
- 공유 제공자 파일도 심볼릭 링크를 따르지 않고 크기를 스트리밍 중 재검사합니다.
- 공유 확장은 App Sandbox 안에서 동작합니다. 호스트는 사용자가 고른 Finder 파일을 원위치
  교체해야 하므로 sandbox 밖이지만 네트워크 및 위험한 Hardened Runtime
  예외 entitlement는 없습니다.
- `com.apple.provenance`와 `com.apple.macl`은 새 파일에 macOS가 관리하는 보안 속성이므로
  입력 메타데이터가 아니며 앱이 제거하지 않습니다.

## 접근성 판단의 한계

소스와 실행 화면에서 컨트롤 이름·도움말·키보드 경로·상태 변경 알림을 확인했습니다. 외부 AX
자동화는 이 macOS 세션에서 MetaShield뿐 아니라 Safari·사진 앱이 창 대신 `AXApplication`,
Finder가 창 대신 `AXScrollArea`를 반환해 신뢰할 수 없었습니다. 따라서 법률상 접근성 인증이나
VoiceOver 실사용 통과를 주장하지 않습니다. 공개 전 VoiceOver, 키보드만 사용, 화면 확대,
대비 증가, 동작 줄이기를 사람이 확인해야 합니다.

## 알려진 제한과 배포 전 확인 항목

1. **미공증 배포:** 최초 실행 때 `개인정보 보호 및 보안 > 확인 없이 열기`가 필요하며 관리형
   Mac에서는 실행이 불가능할 수 있습니다.
2. **사진 권한 재요청:** ad-hoc 서명은 안정적인 Developer ID 코드 요구사항이 없습니다. 새 빌드로
   교체하면 macOS가 사진 추가 권한을 다시 요청할 수 있습니다. 최초 권한 창은 운영체제 UI라
   숨기거나 자동 승인할 수 없습니다. 공유 확장 권한과 호스트 앱 권한은 별도 TCC 주체입니다.
3. **사진 무응답 처리:** 권한 또는 가져오기 서비스가 응답하지 않으면 60초 뒤 임시 파일을 지우고
   종료합니다. 헤드리스 `다음으로 편집` 실패는 창을 띄우지 않으므로 새 이미지가 없으면 사용자가
   시스템 설정의 사진 권한을 확인해야 합니다.
4. **지원/법적 문서:** Apache-2.0 `LICENSE`, `SECURITY.md`, `PRIVACY.md` 연락처를 넣어 해결했습니다.
5. **별도 Mac QA:** 현재 Mac의 격리 복사본으로 신규 설치를 모사했지만 macOS 13 실기와 완전히
   깨끗한 별도 Mac 시험은 하지 못했습니다.
6. **Finder 호환성:** AppKit Services 표시 위치와 등록 시점은 macOS 업데이트에서 바뀔 수 있으므로
   주요 macOS 버전마다 다시 확인해야 합니다. 설치 뒤 앱을 한 번 실행해야 등록됩니다.
7. **보장 경계:** 보이는 RGB 픽셀에 숨긴 임의 스테가노그래피까지 이미지를 유지하며 제거한다는
   보장은 기술적으로 불가능합니다.
8. **사진 원본:** 사진 앱에서는 정리된 새 항목을 추가하고 기존 항목을 자동 삭제하지 않습니다.
   이는 더 넓은 사진 권한과 데이터 손실을 피하기 위한 의도된 동작입니다.
9. **격리 속성 제거:** 원위치 교체 시 `com.apple.quarantine`이 새 파일로 승계되지 않습니다.
   이미지라 실행 위험은 없지만 다운로드 출처 표시는 사라집니다.
10. **최악 메모리:** 비압축성 40 MP(약 119 MB) 입력에서 최대 RSS 약 1.37 GB, peak footprint
   약 923 MB를 측정했습니다. 위 성능 표본은 압축이 잘 되는 입력 기준입니다.
11. **의도된 표현 변경:** 알파는 흰색으로 합성되고 색상 프로파일·Finder 태그·설명·사용자 xattr은
   보존하지 않습니다. PNG 원위치 처리는 되돌릴 수 없으므로 백업 안내가 필요합니다.

## 최종 직접 배포물

- `outputs/MetaShield-0.3.8-direct.dmg`
- SHA-256: `325692a02967e31f3105a337bf8008a89802992e4362dafe01a1b8defd616ff1`

0.3.1 배포물은 위 알파 결함 때문에 배포 대상에서 제외하고 `outputs/archive-pre-0.3.2/`로 옮겼습니다.

일반 사용자에게는 개발용 ZIP이 아니라 위 DMG와 일치하는 `.sha256`을 HTTPS로 함께 배포해야
합니다. 베타 표시를 유지하고 첫 외부 사용자의 설치·사진 권한·Finder 서비스 결과를 받아야
합니다.

## 0.3.8 감사 이후 변경 사항 (0.3.9 배포 완료 / 0.4.0 준비, 배포 전 재감사 필요)

위의 감사 결과는 0.3.8 (build 21) 기준입니다. 이후 main에 반영된 0.3.9 (build 22) 준비
변경은 다음과 같으며, 릴리스 전 아래의 재검증을 마쳐야 합니다.

- 코드 구조: 앱 타깃 단일 `main.swift`(1,908줄)를 역할별 8개 파일로 분리. 동작 변경 없이
  자체 테스트 20/20 유지. `OutputNaming`으로 앱·공유 확장·CLI의 출력 이름 규칙을 통합.
- 수정: 검증 DMG 다운로드가 네트워크 중단으로 실패할 때 체크섬 불일치("설치하지 마세요")로
  잘못 보고하던 문제를 네트워크 오류로 구분하고 부분 파일을 정리.
- 방어: `PNGInspector` 진입점에서 `Data` 슬라이스를 0 기준으로 재정렬해 절대 오프셋 파싱의
  잠재 결함을 차단. CLI는 알 수 없는 `-` 옵션을 파일 경로로 취급하지 않음.
- 입력 경로 추가: `CFBundleDocumentTypes`(`public.image`, `Editor` 역할, `Alternate` 등급)
  선언으로 Dock 드래그와 `다음으로 열기`가 기존 헤드리스 파일 열기 경로에 연결됨.
- 접근성: 상태·업데이트 문구의 VoiceOver 낭독(announcementRequested, 앱·공유 확장), 드롭
  영역 버튼화(클릭·Space·Return·포커스 링·accessibilityPerformPress), 동작 줄이기 시 스피너
  생략, 대비 증가 시 실선 테두리, 업데이트 레이블 대비 상향.
- 신규(기본 꺼짐): `백그라운드 처리 완료 알림`. 창 없이 처리된 결과를 로컬 알림으로 요약.
  알림 본문에는 파일 이름을 넣지 않음(잠금 화면·알림 센터·화면 공유 노출 경로). 권한 요청은
  옵션을 켜는 대화형 순간에만.
- 문구: `사진 권한 미리 연결… (선택)`으로 변경해 선택 단계임을 명시. 관련 문서 동기화.
- CI: GitHub Actions에서 strict format lint, Debug/Release warnings-as-errors 빌드, 자체
  테스트 20종을 push/PR마다 실행. 릴리스 서명은 오프라인 키 유지를 위해 CI에 넣지 않음.

### 0.4.0에서 추가한 AVIF 변환

용량 절감과 웹 업로드를 위해 AVIF 사본 생성을 추가했습니다. WebP는 채택하지 않았습니다 —
Apple ImageIO가 WebP 인코딩을 지원하지 않아 libwebp 번들이 필요한데, 그러면 "제3자 코드를
번들하지 않는다"는 이 앱의 공급망 전제가 깨지고 이미지 파서 공격면이 늘어납니다. AVIF는
ImageIO 네이티브이며 실측에서 같은 이미지 기준 JPEG보다 33~66% 작았습니다.

- **원본을 절대 교체하지 않습니다.** AVIF는 항상 새 파일만 만듭니다. MetaShield의 유일한
  비가역 동작은 이름에 그렇게 적힌 원위치 PNG 명령 하나로 유지됩니다.
- 인코더에 넘기는 이미지는 PNG 경로와 **동일한 파이프라인** 산출물입니다(단일 프레임,
  완전 불투명 8-bit sRGB, 원시 샘플 재구성). 알파 LSB 페이로드는 인코딩 전에 파괴됩니다.
- 저장한 파일을 다시 읽어 ① 이미지 1개 ② 예상 크기 ③ 알파 없음 ④ EXIF·GPS·IPTC·maker note
  등 서술 메타데이터 컨테이너 부재를 확인한 뒤에만 공개합니다. Apple 인코더가 쓰는 구조
  정보(방향, 타일 크기)만 `{TIFF}`에 허용합니다.
- **보장 수준이 PNG보다 약합니다.** AVIF에는 "IHDR·IDAT·IEND만, zlib 스트림 검증"에 해당하는
  청크 수준 정규화가 없습니다. 보장은 "무엇으로 디코딩되고 어떤 메타데이터를 보고하는가"
  수준이며, 컨테이너의 모든 바이트를 규정하지 않습니다. README에 별도 절로 분리해 명시했습니다.
- **AVIF 인코딩은 macOS 26 이상에서만 가능합니다.** GitHub의 macOS 14·15·26 러너에서 실측한
  결과 14·15는 AVIF 디코딩만 되고 인코딩은 불가였습니다(HEIC 인코딩은 전 버전 가능).
  앱 최소 지원이 macOS 13이므로 OS 버전 비교가 아니라 `CGImageDestinationCopyTypeIdentifiers`로
  런타임에 확인하고, 불가하면 전용 오류로 즉시 실패하며 부분 파일을 남기지 않고 원본도
  건드리지 않습니다. 이 회귀는 로컬(macOS 26)에서는 드러나지 않았고 CI가 잡았습니다.
- AVIF는 항상 손실 압축입니다. ImageIO에 무손실 모드가 없고 품질 1.0은 인코딩이 실패하므로
  사용 범위를 0.3~0.95로 클램프합니다.
- Finder 서비스는 3개가 되었고 각 항목 이름이 동작을 그대로 말합니다. 압축률은 메인 창의
  슬라이더이며, 파일이 얼마나 작아지는지만 바꾸고 교체 여부는 바꾸지 않습니다. 숨은 설정이
  비가역 동작의 의미를 바꾸는 구조를 의도적으로 피했습니다.
- 자체 테스트 4종을 추가해 24/24가 되었습니다: 메타데이터 미유출(원본 EXIF 문자열이 출력
  바이트 어디에도 없음), 기존 파일 미덮어쓰기, 품질 범위 클램프, 다중 프레임 거부.

릴리스 전 남은 재검증:

1. VoiceOver 실사용 시험 — 드롭 완료 낭독, 드롭 영역의 키보드(클릭·Space) 조작. 위 표의
   "접근성 코드 조건부 통과" 항목을 실사용 검증으로 닫는 조건.
2. 백그라운드 알림 실기기 확인 — 토글·권한 흐름, 완료/실패 요약, 파일 이름 미포함.
3. Dock 드래그·`다음으로 열기` 실기기 확인 — 헤드리스 처리·창 0개·기본 앱 미승격.
4. 새 DMG에 대한 무결성·중첩 서명·Universal 2·Gatekeeper 검사와 sanitizer 3종 재실행.
5. AVIF 서비스 3종의 실제 Finder 호출 확인(원본 보존, 새 파일 생성, 창 0개), 그리고
   실제 일러스트 표본에서의 품질·용량 확인. AVIF 출력은 구형 브라우저·Safari 16.4 미만에서
   표시되지 않으므로 배포 문서에 호환성 한계를 함께 안내해야 합니다.
