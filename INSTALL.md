# MetaShield 설치 및 사용

## 설치

1. 배포자가 제공한 `MetaShield-x.y.z-direct.dmg`와 `.sha256`을 같은 HTTPS 다운로드 페이지에서 받습니다.
2. 필요하면 터미널에서 `shasum -a 256 MetaShield-x.y.z-direct.dmg` 결과를 배포된 체크섬과 비교합니다.
3. DMG를 열고 `MetaShield.app`을 `Applications` 바로가기로 드래그합니다.
4. 응용 프로그램 폴더에서 MetaShield를 한 번 실행합니다. 이 무료 직접 배포본은 Apple 공증을
   사용하지 않으므로 macOS가 개발자를 확인할 수 없다는 경고를 표시하는 것이 정상입니다.
5. 실행이 차단되면 `시스템 설정 > 개인정보 보호 및 보안`의 보안 영역에서 `확인 없이 열기`를
   누르고 macOS 인증 후 다시 엽니다. 터미널에서 `xattr`을 삭제할 필요는 없습니다.
6. Finder 우클릭 메뉴를 쓰려면 앱의 `Finder 빠른 동작 설치/복구`를 한 번 누릅니다.

## 사용

- Finder: 이미지 선택 → 우클릭 → 빠른 동작 → `MetaShield 메타데이터 완전 제거`.
- PNG: 검증이 끝난 뒤 같은 경로의 원본을 교체합니다. 이 동작은 되돌리기 확인창이 없습니다.
  투명도는 흰색 배경으로 합성되고 Finder 태그·설명·사용자 확장 속성·원본 색상 프로파일은
  보존하지 않으며 파일 날짜가 바뀔 수 있습니다. 되돌려야 할 파일은 먼저 백업하세요.
- JPEG·HEIC·WebP 등: 원본 옆에 이름이 겹치지 않는 `.clean.png`를 만듭니다.
- 사진 앱: `공유 > MetaShield`를 사용하면 macOS 공유 패널 안의 진행 화면이 잠깐 표시된 뒤
  정리된 PNG를 새 사진으로 추가합니다. `다음으로 편집 > MetaShield`와 파일 전달 방식은
  MetaShield 메인 창 없이 처리합니다. 사진 보관함의 기존 항목은 안전상 자동 삭제하지 않습니다.
- 첫 사진 앱 사용 때 macOS의 “사진 추가” 권한 창이 나타날 수 있습니다. Developer ID 없이
  서명된 새 버전으로 교체하면 macOS가 이 권한을 다시 요청할 수도 있습니다.

Finder 빠른 동작과 사진 앱의 파일 전달은 성공·실패 여부와 관계없이 MetaShield 메인 창을
띄우지 않습니다. 공유 확장은 macOS 공유 패널 자체를 숨길 수 없으며 오류는 그 패널 안에
표시됩니다. `다음으로 편집` 방식에서 새 이미지가 생기지 않으면 시스템 설정의 사진 권한을
확인하세요.

## 업데이트와 제거

업데이트는 새 DMG의 앱을 `/Applications/MetaShield.app` 위에 교체한 뒤 빠른 동작
설치/복구를 다시 누릅니다. 제거는 앱과
`~/Library/Services/MetaShield 메타데이터 완전 제거.workflow`를 휴지통으로 옮깁니다.
이미 만든 PNG와 사진 보관함 항목은 자동으로 삭제되지 않습니다.
