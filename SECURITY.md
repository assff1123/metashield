# 보안 정책

MetaShield는 개인이 유지보수하는 무료 도구입니다. 대응은 최선 노력 기준이며
상업적 지원이나 응답 시간 보장(SLA)은 없습니다.

## 지원 버전

최신 릴리스 한 개만 수정 대상입니다. 이전 버전에는 백포트하지 않습니다.

| 버전 | 지원 |
|---|---|
| 0.3.5 | 예 |
| 0.3.4 이하 | 아니요 (최신 릴리스로 교체) |

## 취약점 신고

**공개 이슈에 취약점을 적지 마세요.** GitHub의 비공개 신고 창구를 사용해 주세요.

1. 저장소 상단 [Security 탭](https://github.com/assff1123/metashield/security)으로 이동
2. `Report a vulnerability` 를 눌러 비공개 권고문을 작성

포함해 주시면 좋은 내용: macOS 버전, MetaShield 버전과 빌드 번호, 재현 절차,
가능하면 재현용 입력 파일(민감한 개인 이미지는 보내지 마세요. 합성 예제로 충분합니다).

## 범위

다음은 취약점으로 다룹니다.

- 정리된 출력에 원본의 메타데이터나 은닉 데이터가 남는 경우
- 의도하지 않은 파일이 덮어써지거나 손상되는 경우, 원본 보존 실패
- 이미지 파싱 과정의 메모리 안전 문제, 크래시, 자원 고갈
- 새 버전 확인 기능이 고정된 주소 밖으로 요청을 보내거나 응답으로 동작이 바뀌는 경우
- 빠른 동작·서비스·공유 확장 경로의 권한 우회

다음은 이미 문서화된 설계상 한계이며 취약점으로 다루지 않습니다.
자세한 근거는 `AUDIT.md` 와 `README.md` 의 보장 경계를 참고하세요.

- 보이는 RGB 픽셀에 심은 임의 스테가노그래피가 남는 것.
  이미지를 유지하면서 이를 제거하는 것은 기술적으로 불가능합니다.
- 양자화 버킷을 넘는 알파 차이가 결과에 반영되는 것. 이는 실제로 눈에 보이는
  투명도 차이이며 위 항목과 같은 범주입니다.
- Apple 공증을 받지 않아 최초 실행 때 Gatekeeper 경고가 뜨는 것
- PNG 원위치 교체가 되돌릴 수 없다는 것 (문서화된 의도된 동작)
- 이미 사용자 권한으로 코드 실행이 가능한 공격자가 할 수 있는 일

## 배포물 검증

각 릴리스에는 DMG와 같은 이름의 `.sha256` 파일이 함께 올라갑니다.

```sh
shasum -a 256 MetaShield-0.3.5-direct.dmg
```

출력이 릴리스 페이지의 값과 다르면 설치하지 마세요.

이 배포본은 Apple Developer ID 서명과 공증을 사용하지 않는 ad-hoc 서명본입니다.
따라서 코드 서명으로 개발자 동일성을 확인할 수 없고, 신뢰 근거는 위 체크섬과
이 저장소뿐입니다. 앱은 새 버전을 **확인만** 하며 직접 내려받거나 설치하지 않습니다.
