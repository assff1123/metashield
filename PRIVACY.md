# MetaShield 개인정보 처리 안내

최종 수정: 2026-08-23

MetaShield는 선택한 이미지를 사용자의 Mac 안에서만 디코딩·재인코딩합니다.

## 수집 및 전송

- 개발자 서버, 분석 서비스, 광고 서비스로 이미지나 메타데이터를 전송하지 않습니다.
- 계정, 기기 식별자, 연락처, 위치, 사용 기록을 수집하지 않습니다.
- 자동 업데이트(내려받기·설치) 기능은 없습니다.
- 유일한 네트워크 기능은 **기본으로 꺼져 있는 새 버전 확인**입니다. 아래를 참고하세요.

## 새 버전 확인 (선택, 기본 꺼짐)

앱 창의 `새 버전 확인 (GitHub)` 를 켠 경우에만 동작합니다.

- 대화형으로 앱을 직접 열었을 때 하루에 최대 한 번
  `https://api.github.com/repos/assff1123/metashield/releases/latest` 를 조회합니다.
- 확인을 켜두면 창 없이 처리되는 경로(Finder 서비스·사진 앱)에서도 처리를 마친 뒤
  하루 1회 조회할 수 있습니다. 단 **알림 권한을 이미 허용한 경우에만** 조회하며, 이 경로에서는
  알림 권한을 새로 묻지 않습니다. 확인이 꺼져 있으면 어떤 경로에서도 네트워크를 쓰지 않습니다.
- 새 버전이 있을 때 사용자가 '검증된 DMG 받기'를 누르면, 그때만 GitHub에서 manifest·서명·
  DMG를 내려받습니다. 저장 위치는 다운로드 폴더이며 **앱이 설치하거나 실행하지 않습니다.**
  브라우저로 받은 것과 같은 격리 속성을 파일에 붙여 최초 실행 확인 절차를 유지합니다.
- 새 버전이 있으면 macOS 알림 배너를 띄웁니다. 배너를 누르면 MetaShield 창이 열리고,
  서명·체크섬 검증 다운로드 경로를 사용할 수 있습니다. 알림에는 주소가 들어 있지 않습니다.
- 보내는 정보는 이 요청 자체뿐입니다. 즉 사용자의 IP 주소, 요청 시각, 그리고
  `MetaShield/<버전> (update check)` 형태의 User-Agent 가 GitHub 에 남습니다.
  계정·기기 식별자·처리한 이미지 정보는 보내지 않습니다.
- 응답에서는 버전 번호(`tag_name`) 하나만 읽고, `0.4.0` 형태의 숫자가 아니면 버립니다.
  내려받기 주소와 릴리스 페이지 주소는 앱에 고정되어 있어 서버 응답으로 바꿀 수 없습니다.
- 조회 주소의 리디렉션은 따르지 않으며 응답 수신량이 512 KiB를 넘으면 즉시 중단합니다.
- 이전에 확인한 것보다 낮은 버전을 최신이라고 알려오면 다운로드를 제안하지 않고 경고를
  표시합니다. 이를 위해 지금까지 확인한 가장 높은 버전 번호 하나를 이 Mac에만 저장하며,
  외부로 보내지 않습니다.
- 새 버전이 있으면 안내와 버튼만 표시합니다. 검증 다운로드 버튼은 서명과 체크섬을 확인한
  DMG를 다운로드 폴더에 저장하고, 앱이 직접 설치하거나 실행하지 않습니다. 릴리스 페이지는
  별도 버튼으로만 엽니다.
- 쿠키와 캐시를 쓰지 않으며(ephemeral 세션), 실패하면 조용히 무시합니다.

## 백그라운드 처리 완료 알림 (선택, 기본 꺼짐)

앱 창의 `백그라운드 처리 완료 알림` 을 켠 경우에만 동작합니다.

- 창 없이 처리되는 경로(Finder 서비스·Dock 드래그·다음으로 열기)가 끝나면 macOS 로컬
  알림으로 결과 요약을 표시합니다. 이 기능은 네트워크를 사용하지 않으며 기기 밖으로
  아무것도 보내지 않습니다.
- 알림 본문에는 성공 개수와 실패 사유만 담고 **파일 이름은 담지 않습니다.** 알림은 잠금
  화면·알림 센터 기록·화면 공유에 노출될 수 있기 때문입니다. 파일 이름은 앱 창 안에서만
  표시합니다.
- 알림 권한은 이 옵션을 켜는 대화형 순간에만 요청하며, 창 없이 처리되는 경로 자체는
  권한을 묻지 않습니다.

## Finder 서비스 메뉴 초기 상태

최초 실행 때 한 번만, AVIF 메뉴 2개를 꺼진 상태로 기록합니다. 이때 사용하는 저장소는 macOS
시스템 설정의 서비스 체크박스가 쓰는 것과 같은 `pbs` 환경설정이며, 다른 앱의 서비스 설정은
건드리지 않습니다. 이후에는 다시 쓰지 않으므로 사용자가 나중에 켠 항목을 앱이 되돌리지
않습니다. 네트워크와 무관하며 기기 밖으로 아무것도 보내지 않습니다.

## 사진 보관함

호스트 앱은 실행만으로 사진 권한을 요청하지 않습니다. 사용자가 `사진 앱 권한 연결…`을 누르고
시스템 공유 창에서 MetaShield를 선택하면 사진 공유 확장이 사진 추가 전용 권한을 요청합니다.
이 설정 요청에 쓰는 1×1 RGB PNG는 메타데이터가 없으며 사진 보관함에 추가하지 않습니다.
`다음으로 편집`처럼 호스트 앱이 직접 처리하는 별도 경로는 처음 사용할 때 자체 권한을 요청할 수
있습니다. 이 권한들은 정리된 PNG를 새 사진으로 추가하는 데만 사용합니다. MetaShield는
PhotoKit으로 기존 사진 보관함을 열람하거나 기존 항목을 자동 삭제하지 않습니다.

## 파일 처리

- 로컬 PNG는 검증이 성공한 경우에만 원본 경로를 새 PNG로 교체합니다.
- 다른 형식은 원본을 유지하고 `.clean.png` 사본을 만듭니다.
- AVIF 변환은 macOS 26 이상에서만 동작하며, 항상 원본을 유지하고 `.clean.avif` 사본만 만듭니다. 변환에는 Apple ImageIO만
  사용하며 제3자 라이브러리를 포함하지 않습니다. AVIF는 손실 압축이므로 픽셀이 원본과
  완전히 같지는 않지만, 메타데이터·알파·확장 속성은 PNG 경로와 동일하게 제거하고 결과를
  다시 읽어 EXIF·GPS·IPTC 등이 없음을 확인한 뒤에만 저장합니다.
- 사진 앱이 관리하는 원본은 교체하지 않고 정리된 새 항목을 추가합니다.
- 처리 중 만든 임시 파일은 완료 또는 실패 후 제거합니다.
- 원본 PNG를 교체하면 원본에 붙어 있던 확장 속성이 새 파일로 승계되지 않습니다.
  여기에는 다운로드 출처 표시인 `com.apple.quarantine`도 포함됩니다.
- macOS가 자체 보안 표시인 `com.apple.provenance`, `com.apple.macl` 등을 새 파일에
  추가할 수 있습니다. 이는 입력 이미지에서 복사한 메타데이터가 아니며 앱이 제거를
  시도하지 않는 운영체제 보안 정보입니다.

## 지원과 문의

- 사용 문의와 버그: https://github.com/assff1123/metashield/issues
- 보안 취약점: 공개 이슈 대신 `SECURITY.md` 의 비공개 신고 절차를 이용해 주세요.

개인이 유지보수하는 무료 도구이며 응답 시간을 보장하지 않습니다.
